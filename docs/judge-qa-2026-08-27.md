# Judge-facing QA — 27 Aug 2026

## Outcome

PASS for current public production build.

## Public repository

- `paulcakeface/webmcp-one` is public.
- GitHub public repository metadata recognises the root licence as **MIT License** (`spdx_id: MIT`).
- Public source includes build instructions, canonical live URL, architecture, judge runbook, threat model, tool contracts and production acceptance evidence.

## Live health

All production health endpoints returned success:

- API — `ok:true`, D1 `database:true`
- Mission — `ok:true`
- Broadband — `ok:true`
- Movers — `ok:true`
- Energy — `ok:true`

## Responsive ordinary-browser QA

Tested at 390×844 mobile viewport in an ordinary browser that does not expose WebMCP.

- ONE Mission Board: no horizontal overflow; hero and service cards collapse to one column; all three provider cards remain inside viewport.
- Northstar: no horizontal overflow; human search controls fit; populated plan cards fit within viewport.
- BoxFox: no horizontal overflow; human quote controls fit; populated moving-slot cards fit within viewport.
- Evergreen: no horizontal overflow; comparison controls fit; all populated tariff cards fit within viewport.

The ordinary browser correctly displays `WebMCP unavailable in this browser`; this is expected. Production WebMCP acceptance is documented separately from a headed Chrome 151 browser with WebMCP testing enabled.

## Accessibility / semantic surface

The Mission Board accessibility tree exposes:

- one H1 for the product thesis;
- H2 current mission;
- H3 provider names and human override;
- labelled slider and Update mission button;
- three real top-level provider links carrying the mission ID;
- Human Approval, What Your Agent Shared and Mission Ledger sections.

## Console

No console errors or warnings were observed on the clean canonical Mission page during ordinary-browser QA.

## Submission requirements rechecked

On 27 Aug 2026, the current Devpost requirements still ask for:

- working live URL;
- public source repository with detectable open-source licence;
- project description explaining WebMCP fit, better UX, new human+agent capability and brief implementation;
- public YouTube demo under three minutes, with audio;
- a complete, coherent product experience rather than a technical proof of concept.

The project is currently green on every code/live/repository requirement. Remaining submission work is packaging: final description, video, final ChatGPT desktop Site Tools acceptance if available, and submission freeze.
